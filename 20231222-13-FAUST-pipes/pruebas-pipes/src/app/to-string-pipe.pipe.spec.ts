import { ToStringPipePipe } from './to-string-pipe.pipe';

describe('ToStringPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ToStringPipePipe();
    expect(pipe).toBeTruthy();
  });
});
